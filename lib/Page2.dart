import 'package:flutter/cupertino.dart';

class Page2 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        '이용 서비스', // 이 부분의 글자만 다르게 작성
        style: TextStyle(fontSize: 40),
      ),
    );
  }
}
